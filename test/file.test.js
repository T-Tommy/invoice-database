const test = QUnit.test;

QUnit.module('Module name');

test('Test name', assert => {
    // Arrange
    const expected = 

    const input =

    // Act
    const result = 

    // Assert
    assert.equal(result, expected);
});